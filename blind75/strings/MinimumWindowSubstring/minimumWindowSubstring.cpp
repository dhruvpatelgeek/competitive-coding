#include <bits/stdc++.h>

using namespace std;

class Solution {
	const int ascii_sz=128;
public:
    string minWindow(string s, string t) {
	const int n=(int)s.size();

    }

};

void solve(){
	string s,t; cin>>s>>t;
	Solution sol;
	cout<<sol.minWindow(s,t);
}

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int t; cin>>t;
	while(t--){
		solve(); cout<<endl;
	}
}
